const BASE_URL = "https://api.github.com";
const USERS_ENDPOINT = "/search/users";
const REPOSITORIES_ENDPOINT = "/search/repositories";

const API = { BASE_URL, USERS_ENDPOINT, REPOSITORIES_ENDPOINT };

export default API;
